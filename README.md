# WIP: Pandoc Experimental SSG

Pandoc-powered markdown to static-website script. Requires [pandoc](https://pandoc.org/installing.html).

```sh
chmod +x ./build.sh # make it executable
./build.sh # run it
```